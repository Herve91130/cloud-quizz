{% extends "base.html.twig" %}

{% block title %}{{ themeQuizz.titre }}{% endblock %}

{% block body %}
<main>
    <div class="container">
        <div class="mt-5 pt-2">
            <h3 class="d-flex justify-content-center">Choisissez un thème et testez vos connaissances !</h3>
        </div>
        <div class="row mt-5 g-4 g-lg-5">
            {% for jeux_quizz in jeux_quizzs %}
                <div class="col-12 col-md-6 col-lg-3">
                    <div class="cartemove card">
                        <img src="{{ asset(jeux_quizz.image) }}" class="card-img-top" alt="{{ jeux_quizz.ImageAlt }}">
                        <div class="card-body">
                            <p class="card-text"></p>
                            <a href="{{ path('jeux_quizz_page', {id: jeux_quizz.id}) }}" class="btn btn-primary d-flex justify-content-center">{{ jeux_quizz.Jeux }}</a>
                            <a href="{{ path('jeux_forum', {id: jeux_quizz.id }) }}" class="mt-2 btn btn-primary d-flex justify-content-center">Forum</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</main>
{% endblock %}
