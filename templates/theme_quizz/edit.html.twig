{% extends 'base.html.twig' %}

{% block title %}Modifier ThemeQuizz{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Modifier ThemeQuizz</h1>

    {{ include('theme_quizz/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('theme_quizz_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('theme_quizz/_delete_form.html.twig') }}
{% endblock %}
