{% extends 'base.html.twig' %}

{% block title %}ThemeQuizz{% endblock %}

{% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
            <link rel="stylesheet" href="{{ asset("css/nuage.css") }}">
{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">ThemeQuizz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theme_quizz.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ theme_quizz.titre }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ theme_quizz.lien }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ theme_quizz.image }}</td>
            </tr>
            <tr>
                <th>Image_alt</th>
                <td>{{ theme_quizz.imageAlt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('theme_quizz_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('theme_quizz_edit', {'id': theme_quizz.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('theme_quizz/_delete_form.html.twig') }}
{% endblock %}
