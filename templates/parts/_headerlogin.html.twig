<header>
    <nav class="navbar navbar-expand navbar-dark bg-dark navbardlogin"></nav>
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
        <div class="container-fluid">
            <a class="navbar-brand ms-5" href="{{path('home')}}">
                <h1 class="fw-bold"><span class="m">C</span><span class="j">L</span><span class="b">O</span><span class="r">U</span><span class="m">D</span> <span class="j">Q</span><span class="r">UI</span><span class="m">Z</span><span class="b">Z</span></h1>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-around" id="navbarSupportedContent">
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item2">
                        <a class="nav-link text-dark" href="{{path('home')}}">Accueil</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item1">
                        <a class="nav-link active text-dark" aria-current="page" href="{{path('moncompte')}}">Mon compte</a>
                    </li>
                    {% else %}
                    <li class="nav-item1">
                        <a class="nav-link active text-dark" aria-current="page" href="{{path('register')}}">Créer un compte</a>
                    </li>
                    {% endif %}
                    <li class="nav-item2">
                        <a class="nav-link text-dark" href="{{path('aproposdenous')}}">À propos de nous</a>
                    </li>
                    <li class="nav-item2">
                        <a class="nav-link text-dark" href="{{path('offrepremium')}}">Offre premium</a>
                    </li>
                    <li class="nav-item2">
                        <a class="nav-link text-dark" href="{{path('boutique')}}">Boutique</a>
                    </li>
                    <li class="nav-item2">
                        <a class="nav-link text-dark" href="{{path('panier')}}"><i class="fas fa-shopping-cart"></i> Panier</a>
                    </li>
                    <li class="nav-item2">
                        <a class="nav-link text-dark" href="{{path('contact')}}">Contactez nous</a>
                    </li>
                </ul>{#
                <form class="d-flex">
                    <input class="form-control" type="search" placeholder="Rechercher..." aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">
                        <i class="fa fa-search"></i>
                    </button>
                </form>#}
            </div>
        </div>
    </nav>
    {% if is_granted('ROLE_MODERATOR') %}
        <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #ffa806;">
            <div class="container-fluid">
                {% if is_granted('ROLE_ADMIN') %}
                <a class="navbar-brand text-danger" href="#">⚠️ Menu Administrateur ⚠️</a>
                {% else %}
                <a class="navbar-brand text-danger" href="#">⚠️ Menu Moderateur ⚠️</a>
                {% endif %}
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav">
                        {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link" href="{{path('user_index')}}">⚙️ Utilisateur</a>
                        {% endif %}
                        <a class="nav-link" href="{{path('commentaires_index')}}">⚙️ Commentaire</a>
                        {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link" href="{{path('produit_boutique_index')}}">⚙️ Produit boutique</a>
                        <a class="nav-link" href="{{path('theme_quizz_index')}}">⚙️ Theme Quizz</a>
                        <a class="nav-link" href="{{path('jeux_quizz_index')}}">⚙️ Jeux Quizz</a>
                        <a class="nav-link" href="{{path('question_quizz_index')}}">⚙️ Question quizz</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>
    {% endif %}
</header>
