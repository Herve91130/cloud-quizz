{% extends 'base.html.twig' %}

{% block title %}Modifier un commentaires{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Modifier un commentaires</h1>

    {{ include('commentaires/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('commentaires_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('commentaires/_delete_form.html.twig') }}
{% endblock %}
