{% extends 'base.html.twig' %}

{% block title %}Modifier une commande{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Modifier une commande</h1>

    {{ include('commande/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('commande_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
