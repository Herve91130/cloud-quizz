<form method="post" action="{{ path('question_quizz_delete', {'id': question_quizz.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question_quizz.id) }}">
    <button class="btn btn-danger mt-3">Supprimer</button>
</form>
